name: Pull Request CI

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

env:
  NODE_VERSION: '22'

permissions:
  contents: read
  pull-requests: write
  checks: write

jobs:
  # Check if files have changed that require CI
  changes:
    name: Check Changes
    runs-on: ubuntu-latest
    outputs:
      src: ${{ steps.filter.outputs.src }}
      tests: ${{ steps.filter.outputs.tests }}
      deps: ${{ steps.filter.outputs.deps }}
      docs-only: ${{ steps.filter.outputs.docs-only }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        
      - name: Check for file changes
        uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            src:
              - 'src/**'
              - 'app/**'
              - 'components/**'
              - 'lib/**'
              - 'prisma/**'
              - 'public/**'
              - '*.config.*'
              - '*.json'
              - '*.ts'
              - '*.tsx'
            tests:
              - 'tests/**'
              - 'playwright.config.*'
              - 'jest.config.*'
            deps:
              - 'package.json'
              - 'package-lock.json'
              - 'prisma/schema.prisma'
            docs-only:
              - '*.md'
              - 'docs/**'
              - '.vscode/**'
              - '.idea/**'
              - 'README*'
              - 'LICENSE*'
              - '.gitignore'

  # Enforce a Conventional Commit PR title. Squash-merge uses the PR title as the
  # commit subject on `main`, which is git-cliff's input for release notes.
  pr-title:
    name: PR Title (Conventional)
    runs-on: ubuntu-latest
    steps:
      - name: Validate PR title
        uses: amannn/action-semantic-pull-request@v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            perf
            refactor
            docs
            chore
            ci
            test
            build
            style
            revert
          requireScope: false
          subjectPattern: ^(?![A-Z]).+$
          subjectPatternError: >-
            The subject "{subject}" must start with a lowercase letter
            (e.g. "fix: handle empty upload", not "Fix: ...").

  # Security and Code Quality Checks
  lint-and-type-check:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.src == 'true' || needs.changes.outputs.tests == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: TypeScript type check
        run: npm run typecheck

      - name: Check formatting (if configured)
        run: |
          if [ -f ".prettierrc" ] || [ -f ".prettierrc.json" ] || [ -f "prettier.config.js" ]; then
            npx prettier --check "src/**/*.{ts,tsx,js,jsx,json,css,md}"
          fi
        continue-on-error: true

  # Dependency Security Audit
  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.deps == 'true' || needs.changes.outputs.src == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Audit dependencies
        run: npm audit --audit-level moderate

  # Secret Scanning — fails the gate on verified secrets in the PR diff
  secret-scan:
    name: Secret Scan
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.src == 'true' || needs.changes.outputs.tests == 'true' || needs.changes.outputs.deps == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Scan PR diff for secrets
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.pull_request.base.sha }}
          head: ${{ github.event.pull_request.head.sha }}
          extra_args: --only-verified

  # Static analysis — CodeQL for JS/TS. Report-only: findings surface in the
  # Security tab / as PR code-scanning annotations but do NOT gate the merge
  # (deliberately absent from ci-status). Lets us watch signal before blocking.
  codeql:
    name: CodeQL Analysis
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.src == 'true'
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: javascript-typescript

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: '/language:javascript-typescript'

  # Vitest unit tests (pure logic, fast)
  unit-tests:
    name: Unit Tests (Vitest)
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.src == 'true' || needs.changes.outputs.tests == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Run unit tests
        run: npm run test:unit

  # Build Test
  build:
    name: Build Application
    runs-on: ubuntu-latest
    needs: [changes, lint-and-type-check]
    if: needs.changes.outputs.src == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Setup test environment
        run: |
          echo "NEXTAUTH_SECRET=test-secret-for-ci-builds-minimum-32-characters" >> .env.local
          echo "NEXTAUTH_URL=http://localhost:3000" >> .env.local
          echo "OPENAI_API_KEY=sk-test-key-placeholder" >> .env.local
          echo "NEXT_TELEMETRY_DISABLED=1" >> .env.local

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Build application
        run: npm run build
        env:
          NODE_ENV: production

      - name: Check build output
        run: |
          if [ ! -d ".next" ]; then
            echo "::error::Build failed - .next directory not found"
            exit 1
          fi

  # Run Tests
  test:
    name: Integration Tests (Playwright + testcontainers)
    runs-on: ubuntu-latest
    needs: [changes, build]
    if: needs.changes.outputs.src == 'true' || needs.changes.outputs.tests == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Setup test environment
        run: |
          echo "NEXTAUTH_SECRET=test-secret-for-ci-builds-minimum-32-characters" >> .env.local
          echo "NEXTAUTH_URL=http://localhost:3000" >> .env.local
          echo "OPENAI_API_KEY=sk-test-key-placeholder" >> .env.local
          echo "NEXT_TELEMETRY_DISABLED=1" >> .env.local

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run integration tests
        run: npm run test:ci
        env:
          CI: true

      - name: Upload test results
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: playwright-report-pr-${{ github.event.pull_request.number }}
          path: playwright-report/
          retention-days: 7

      - name: Add test results to summary
        if: always()
        run: |
          echo "## 🧪 Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f "playwright-report/index.html" ]; then
            echo "✅ Playwright HTML report generated" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📊 Download the \`playwright-report-pr-${{ github.event.pull_request.number }}\` artifact to view detailed test results" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ No test report generated" >> $GITHUB_STEP_SUMMARY
          fi

  # Fast docs-only check
  docs-check:
    name: Documentation Check
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.docs-only == 'true' && needs.changes.outputs.src != 'true' && needs.changes.outputs.tests != 'true' && needs.changes.outputs.deps != 'true'
    steps:
      - name: Docs only changes detected
        run: |
          echo "✅ Only documentation files changed - skipping code tests"
          echo "This PR contains documentation-only changes and doesn't require comprehensive testing."

  # PR Comment with Status
  pr-comment:
    name: PR Status Comment
    runs-on: ubuntu-latest
    needs: [lint-and-type-check, security-audit, secret-scan, unit-tests, build, test]
    if: always()
    permissions:
      pull-requests: write
    steps:
      - name: Comment PR Status
        uses: actions/github-script@v9
        with:
          script: |
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(comment =>
              comment.user.type === 'Bot' &&
              comment.body.includes('## CI Status')
            );

            const lintStatus = '${{ needs.lint-and-type-check.result }}';
            const securityStatus = '${{ needs.security-audit.result }}';
            const secretStatus = '${{ needs.secret-scan.result }}';
            const unitStatus = '${{ needs.unit-tests.result }}';
            const buildStatus = '${{ needs.build.result }}';
            const testStatus = '${{ needs.test.result }}';

            const statusEmoji = (status) => {
              switch(status) {
                case 'success': return '✅';
                case 'failure': return '❌';
                case 'skipped': return '⏭️';
                default: return '⏸️';
              }
            };

            const body = `## CI Status

            | Check | Status |
            |-------|--------|
            | Lint & Type Check | ${statusEmoji(lintStatus)} ${lintStatus} |
            | Security Audit | ${statusEmoji(securityStatus)} ${securityStatus} |
            | Secret Scan | ${statusEmoji(secretStatus)} ${secretStatus} |
            | Unit Tests (Vitest) | ${statusEmoji(unitStatus)} ${unitStatus} |
            | Build | ${statusEmoji(buildStatus)} ${buildStatus} |
            | Integration Tests | ${statusEmoji(testStatus)} ${testStatus} |
            
            ${testStatus === 'success' ? '### ✅ All checks passed!' : '### ⚠️ Some checks require attention'}
            
            > **Note**: PR CI runs fast unit tests only. Comprehensive workflow tests run after merge.
            
            [View Full Workflow Run](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
            
            ---
            *Last updated: ${new Date().toUTCString()}*`;
            
            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

  # Final Status Check
  ci-status:
    name: CI Status
    runs-on: ubuntu-latest
    needs: [changes, pr-title, lint-and-type-check, security-audit, secret-scan, unit-tests, build, test, docs-check]
    if: always()
    steps:
      - name: Check CI Status
        run: |
          # PR title gate applies to every PR (including docs-only) — it feeds release notes.
          if [ "${{ needs.pr-title.result }}" == "failure" ]; then
            echo "::error::PR title is not a valid Conventional Commit"
            exit 1
          fi

          # For docs-only changes (no src, tests, or deps changes)
          if [ "${{ needs.changes.outputs.docs-only }}" == "true" ] && \
             [ "${{ needs.changes.outputs.src }}" != "true" ] && \
             [ "${{ needs.changes.outputs.tests }}" != "true" ] && \
             [ "${{ needs.changes.outputs.deps }}" != "true" ]; then
            echo "✅ Documentation-only changes - no code testing required!"
            exit 0
          fi
          
          # For regular changes, check all jobs that should have run
          failed_jobs=()
          
          # Check lint and type check
          if [ "${{ needs.lint-and-type-check.result }}" == "failure" ]; then
            failed_jobs+=("Lint & Type Check")
          fi
          
          # Check build (only if src changed)
          if [ "${{ needs.changes.outputs.src }}" == "true" ] && [ "${{ needs.build.result }}" == "failure" ]; then
            failed_jobs+=("Build")
          fi
          
          # Check unit tests (if src or tests changed)
          if ([ "${{ needs.changes.outputs.src }}" == "true" ] || [ "${{ needs.changes.outputs.tests }}" == "true" ]) && [ "${{ needs.unit-tests.result }}" == "failure" ]; then
            failed_jobs+=("Unit Tests")
          fi

          # Check integration tests (if src or tests changed)
          if ([ "${{ needs.changes.outputs.src }}" == "true" ] || [ "${{ needs.changes.outputs.tests }}" == "true" ]) && [ "${{ needs.test.result }}" == "failure" ]; then
            failed_jobs+=("Integration Tests")
          fi
          
          # Check security audit (if deps or src changed)
          if ([ "${{ needs.changes.outputs.deps }}" == "true" ] || [ "${{ needs.changes.outputs.src }}" == "true" ]) && [ "${{ needs.security-audit.result }}" == "failure" ]; then
            failed_jobs+=("Security Audit")
          fi

          # Check secret scan (if src, tests, or deps changed)
          if ([ "${{ needs.changes.outputs.src }}" == "true" ] || [ "${{ needs.changes.outputs.tests }}" == "true" ] || [ "${{ needs.changes.outputs.deps }}" == "true" ]) && [ "${{ needs.secret-scan.result }}" == "failure" ]; then
            failed_jobs+=("Secret Scan")
          fi

          if [ ${#failed_jobs[@]} -gt 0 ]; then
            echo "::error::CI checks failed: ${failed_jobs[*]}"
            exit 1
          fi
          
          echo "✅ All required CI checks passed successfully!"